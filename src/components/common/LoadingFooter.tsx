import { ActivityIndicator } from 'react-native';

/** The spinner shown under a list while the next page loads. */
export const LoadingFooter = () => <ActivityIndicator className="py-6" color="#7A56C0" />;
